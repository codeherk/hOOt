/**
 * Algorithm to check for course name that has the best match to user's requested phrase
 * Uses levenshtein algorithm to check difference between phrases
 * Checks for phrase being a sub string of other words to better increase match
 * 
 * 
 */
var assert = require('assert');

/*
 * Status codes indicating success and other status 
 */

describe('Array', function() {
  describe('getCourses()', function() {
    it('should return -1 when the value is not present', function() {
      assert.equal(getCourses(4), -1);
    });
  });
});

describe('Array', function() {
    describe('getTACourses()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });
  
  describe('Array', function() {
    describe('getAnnouncements()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  describe('Array', function() {
    describe('mapCourses()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  describe('Array', function() {
    describe('coursesToString()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  describe('Array', function() {
    describe('announcementsToString()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  describe('Array', function() {
    describe('formatAssignments()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  describe('Array', function() {
    describe('courseGradesToString()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  describe('Array', function() {
    describe('buildHttpGetOptions()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  describe('Array', function() {
    describe('httpGet()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });
  describe('Array', function() {
    describe('speakError()', function() {
      it('should return -1 when the value is not present', function() {
        assert.equal(getCourses(4), -1);
      });
    });
  });

  
//things to export
module.exports = { FinalWord };